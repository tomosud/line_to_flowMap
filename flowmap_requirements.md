# 線画からフローマップを生成するツール 要件定義

## 1. 目的

線画画像からスケルトンを抽出し、線方向に沿ったフローマップ（0.5基準のベクトルマップ）を生成する。
画像全体に自然に広がる安定した方向場を構築することを目的とする。

------------------------------------------------------------------------

## 2. 入力仕様

-   入力: 線画画像（白背景に黒線を想定）
-   解像度: 任意（推奨 512〜2048px）
-   フォーマット: PNG / JPG

------------------------------------------------------------------------

## 3. 出力仕様

-   出力: フローマップ画像（RGB）
-   R = 0.5 + 0.5 \* Tx
-   G = 0.5 + 0.5 \* Ty
-   B = 任意（0固定または1固定）
-   値域: 0.0〜1.0

------------------------------------------------------------------------

## 4. 処理フロー

### 4.1 二値化

-   Otsu法または適応的二値化
-   必要に応じてモルフォロジーclose処理で線の途切れを補修

### 4.2 スケルトン化

-   1px幅のスケルトンへ変換
-   8近傍接続を前提とする

### 4.3 グラフ構築

各スケルトン画素の次数を算出

-   次数1: 端点
-   次数2: 通常点
-   次数3以上: 分岐点

連結成分ごとに管理する。

------------------------------------------------------------------------

## 5. 線分・閉曲線の扱い

### 5.1 開いた線分

-   端点の座標スコアで開始方向を決定
-   例: score = y \* width + x
-   scoreが小さい方を開始点とする
-   方向を統一してポリラインを構築

### 5.2 閉曲線の検出

条件: - 端点数 = 0 - 分岐点数 = 0

この場合: - 任意の点を開始点として1周トレース -
途切れない連続ポリラインを構築

------------------------------------------------------------------------

## 6. 接線ベクトル計算

各ポリライン点 P\[i\] に対し:

内部点: t = normalize(P\[i+1\] - P\[i-1\])

端点: t = normalize(P\[1\] - P\[0\])

------------------------------------------------------------------------

## 7. ベースベクトル場 V0

まず画像全体に基準ベクトルを設定する。

例: - 固定右向き: (1, 0) - 右下方向: normalize(1, 1)

------------------------------------------------------------------------

## 8. スケルトン方向の安定化

各スケルトン点 s において:

if dot(t, V0(s)) \< 0: t = -t

ベース方向と大きく逆向きにならないよう符号を統一する。

------------------------------------------------------------------------

## 9. 距離ベースの影響拡張

各画素 x に対して:

1.  最寄りスケルトン点 s を取得
2.  距離 d = \|\|x - s\|\|
3.  重み w(d) を計算

例: w = exp(-(d^2)/(2σ^2)) または w = clamp(1 - d/R, 0, 1)

最終ベクトル:

    V(x) = normalize((1 - w) * V0(x) + w * Vs(s))

------------------------------------------------------------------------

## 10. フローマップ変換

最終ベクトル V = (Tx, Ty) を:

    R = 0.5 + 0.5 * Tx
    G = 0.5 + 0.5 * Ty

として保存。

------------------------------------------------------------------------

## 11. 安定化オプション

-   軽いベクトル平滑化
-   分岐点周辺のぼかし
-   端点近接ブリッジ処理

------------------------------------------------------------------------

## 12. 破綻を防ぐ設計思想

-   まず全画素にベース場を定義する
-   スケルトンは局所拘束として作用させる
-   距離減衰で自然にブレンドする
-   向きの符号を統一して反転破綻を防ぐ

------------------------------------------------------------------------

## 13. 実装推奨環境

-   Python
-   OpenCV
-   scikit-image
-   NumPy

------------------------------------------------------------------------

以上
